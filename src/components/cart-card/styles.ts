import styled from "styled-components";
import { Typography, Grid } from "@material-ui/core";


export const TextPlain = styled(Typography)({
  margin: "5px",
});

export const GridPlain = styled(Grid)({
  display: "flex",
  flexDirection: "row",
});