import styled from 'styled-components';

export const Container = styled.main`
  padding: 4rem 1.5rem;
  max-width: 1224px;
  margin: 0 auto;

  h4 {
    font-size: 2rem;
    font-weight: 500;
    text-align: center;
    color: var(--gray-100);
  }

  ul {
    margin-top: 1.25rem;
  }
`;
